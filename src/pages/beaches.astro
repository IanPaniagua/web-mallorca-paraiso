---
import Layout from '../layouts/Layout.astro';
import BeachesSidebar from '../components/BeachesSidebar';

interface Filters {
  search: string;
  region: string;
  town: string;
  type: string;
}

interface Beach {
  name: string;
  image: string;
  description: string;
  region: string;
  town: string;
  type: string;
  services: string[];
  access: string;
  featured: boolean;
}

const regions = [
  "Norte",
  "Sur",
  "Este",
  "Oeste",
  "Bahía de Palma"
];

const towns = [
  "Alcúdia",
  "Pollença",
  "Artà",
  "Capdepera",
  "Manacor",
  "Santanyí",
  "Calvià",
  "Andratx",
  "Palma",
  "Campos"
];

const types = [
  "Cala",
  "Playa"
];

const beaches: Beach[] = [
  {
    name: "Playa de Alcúdia",
    image: "/images/alcudia.jpg",
    description: "Una de las playas más largas de Mallorca, con aguas cristalinas y arena blanca.",
    region: "Norte",
    town: "Alcúdia",
    type: "Playa",
    services: ["Duchas", "Hamacas", "Restaurantes", "Deportes acuáticos"],
    access: "Fácil acceso, parking gratuito",
    featured: true
  },
  {
    name: "Cala Mesquida",
    image: "/images/rock.jpg",
    description: "Extensa playa de arena blanca rodeada de dunas y pinos",
    region: "Este",
    town: "Capdepera",
    type: "Cala",
    services: ["Parking", "Restaurantes", "Tumbonas", "Socorrista"],
    access: "Acceso por carretera asfaltada",
    featured: false
  },
  {
    name: "Cala Varques",
    image: "/images/cala-varques.jpg",
    description: "Cala virgen de aguas cristalinas, perfecta para snorkel",
    region: "Este",
    town: "Manacor",
    type: "Cala",
    services: ["Snorkel", "Acantilados", "Naturaleza"],
    access: "Acceso a pie (15 min)",
    featured: false
  },
  {
    name: "Es Trenc",
    image: "/images/es-trenc.jpg",
    description: "Playa paradisíaca de aspecto caribeño",
    region: "Sur",
    town: "Campos",
    type: "Playa",
    services: ["Chiringuitos", "Tumbonas", "Deportes acuáticos", "Parking"],
    access: "Parking de pago disponible",
    featured: true
  }
];
---

<Layout title="Playas de Mallorca - Calas y Playas Paradisíacas">
  <div slot="header">
    <div class="py-2">
      <h1 class="text-3xl sm:text-4xl md:text-5xl font-display bg-gradient-to-r from-[#0066FF] to-[#00D4FF] bg-clip-text text-transparent mb-4">
        Playas y Calas
      </h1>
      <p class="text-base sm:text-lg text-gray-600 max-w-2xl mx-auto">
        Descubre los rincones más paradisíacos de la costa mallorquina.
      </p>
    </div>
  </div>

  <script is:inline>
    function filterBeaches(filters) {
      console.log('Applying filters:', filters);
      
      const articles = document.querySelectorAll('#beachesGrid article');
      
      articles.forEach(article => {
        const region = article.dataset.region;
        const town = article.dataset.town;
        const type = article.dataset.type;
        const name = article.dataset.name;
        
        const matchesSearch = !filters.search || 
          name.toLowerCase().includes(filters.search.toLowerCase());
        
        const matchesRegion = !filters.region || 
          region === filters.region;
        
        const matchesTown = !filters.town || 
          town === filters.town;
        
        const matchesType = !filters.type || 
          type === filters.type;
        
        const isVisible = matchesSearch && matchesRegion && 
          matchesTown && matchesType;
        
        console.log('Beach:', name, 'Visible:', isVisible);
        article.style.display = isVisible ? 'flex' : 'none';
      });
    }

    // Initialize filters when page loads
    document.addEventListener('DOMContentLoaded', () => {
      console.log('Initializing filters...');
      filterBeaches({ search: '', region: '', town: '', type: '' });
    });
  </script>

  <div class="flex">
    <BeachesSidebar 
      client:load
      regions={regions}
      towns={towns}
      types={types}
    />
    <div id="beachesGrid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 flex-1 p-6">
      {beaches.map((beach) => (
        <article 
          class="bg-white rounded-lg shadow-sm overflow-hidden flex flex-col"
          data-region={beach.region}
          data-town={beach.town}
          data-type={beach.type}
          data-name={beach.name.toLowerCase()}
        >
          <div class="relative">
            <img 
              src={beach.image} 
              alt={`Playa ${beach.name}`}
              class="w-full h-48 object-cover"
            />
            {beach.featured && (
              <span class="absolute top-2 right-2 bg-yellow-400 text-yellow-900 text-xs px-2 py-1 rounded-full">
                Destacada
              </span>
            )}
          </div>
          <div class="p-4">
            <h3 class="text-xl font-display bg-gradient-to-r from-[#0066FF] to-[#00D4FF] bg-clip-text text-transparent mb-2">
              {beach.name}
            </h3>
            <p class="text-gray-600 mb-4">{beach.description}</p>
            <div class="space-y-2 text-sm text-gray-600">
              <p class="flex items-center gap-2">
                <span class="text-[#0066FF]">📍</span>
                {beach.town}, {beach.region}
              </p>
              <p class="flex items-center gap-2">
                <span class="text-[#0066FF]">🏖️</span>
                {beach.type}
              </p>
              <p class="flex items-center gap-2">
                <span class="text-[#0066FF]">🚗</span>
                {beach.access}
              </p>
            </div>
            <div class="mt-4">
              <h4 class="text-sm font-semibold text-gray-500 mb-2">Servicios</h4>
              <div class="flex flex-wrap gap-2">
                {beach.services.map(service => (
                  <span class="bg-gradient-to-r from-[#0066FF]/10 to-[#00D4FF]/10 text-[#0066FF] px-3 py-1 rounded-full text-sm">
                    {service}
                  </span>
                ))}
              </div>
            </div>
          </div>
        </article>
      ))}
    </div>
  </div>

  <section class="bg-gradient-to-b from-[#0066FF]/20 to-[#00D4FF]/20 py-12 px-4 rounded-lg mb-12">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="text-3xl font-display bg-gradient-to-r from-[#0066FF] to-[#00D4FF] bg-clip-text text-transparent mb-6">Consejos para tu visita</h2>
      <div class="grid md:grid-cols-3 gap-6">
        <div>
          <div class="text-2xl mb-2">🌊</div>
          <h3 class="font-display text-lg mb-2">Mejor temporada</h3>
          <p class="text-gray-600">Mayo a Octubre son los meses ideales para disfrutar del mar</p>
        </div>
        <div>
          <div class="text-2xl mb-2">🧴</div>
          <h3 class="font-display text-lg mb-2">Protección</h3>
          <p class="text-gray-600">No olvides protector solar y sombrilla en las calas sin servicios</p>
        </div>
        <div>
          <div class="text-2xl mb-2">⏰</div>
          <h3 class="font-display text-lg mb-2">Horario</h3>
          <p class="text-gray-600">Visita temprano para encontrar mejor parking y menos gente</p>
        </div>
      </div>
    </div>
  </section>
</Layout>

